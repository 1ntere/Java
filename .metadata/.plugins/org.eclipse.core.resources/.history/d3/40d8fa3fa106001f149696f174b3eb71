package com.kh.switchEx;
/*
Switch-Case
	 어떤 값이 switch 안으로 주어지고
	 주어진 값 중에서 일치하는 값이 case에 있다면
	 case에서 어떤 특정 행동을 진행할 수 있도록 설정
	 그런데 주어진 값이 일치하는 case가 없다면 default로 이동하게 됨
	 
	 case는 break가 없으면 나머지 밑에 있는 case와 default에 작성된 코드들이 모두 실행이 되기 때문에
	 case에다가 필요한 기능을 작성했으면 break를 작성해주는 것이 좋음
	 
	 default는 필수로 작성해야하는 것이 아님
	 
	 switch (변수 또는 표현식) {
	 case 값1 :
	 	변수가 값1에 해당하는 내용일 때 실행할 동작 작성;
	 	break; //break 도 필수는 아님
	 case 값2 : case 값3 :
	 	변수가 값2 또는 값3에 해당하는 내용일 때 실행할 동작 작성;
	 	break;
	 default :
	 	모든 case에 해당하는 내용이 없을 때 실행할 동작 작성
	 	break;
	 
	 

*/
public class Switch문 {

}
